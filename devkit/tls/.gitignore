# Self-signed certs generated by `make tls` — never version-controlled.
# Customers who drop in CA-signed certs (cert.pem / key.pem) likewise
# keep them out of git.
*
!.gitkeep
!.gitignore
!README.md
